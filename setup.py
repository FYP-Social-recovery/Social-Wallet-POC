from setuptools import setup, find_packages  
setup(name = 'nodeModel', packages = find_packages())
setup(name='publicContractModel', packages=find_packages())
setup(name='nodeController', packages=find_packages())
setup(name='publicContractController', packages=find_packages())
setup(name = 'keyGenerationController', packages = find_packages())
setup(name = 'segmentation', packages = find_packages())
setup(name = 'normalization', packages = find_packages())
setup(name = 'gabor_filter', packages = find_packages())
setup(name = 'frequency', packages = find_packages())
setup(name = 'orientation', packages = find_packages())
setup(name = 'skeletonize', packages = find_packages())

