from setuptools import setup, find_packages  
setup(name = 'nodeModel', packages = find_packages())
setup(name='publicContractModel', packages=find_packages())
setup(name='nodeController', packages=find_packages())
setup(name='publicContractController', packages=find_packages())
setup(name = 'keyGenerationController', packages = find_packages())
setup(name = 'otp_controller', packages = find_packages())
setup(name = 'email_controller', packages = find_packages())
setup(name = 'fvss_controller', packages = find_packages())